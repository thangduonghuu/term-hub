[package]
name = "termhub"
version = "0.1.0"
description = "TermHub tiles all your terminal sessions into one window as real shells (portable-pty + xterm.js), built for running multiple AI coding agents (Claude Code, Codex, etc.) in parallel. Rename, persist, and reopen sessions, or pop any one open in iTerm/Warp/Windows Terminal. Tauri + React, MIT licensed."
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "termhub_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
portable-pty = "0.8"
rusqlite = { version = "0.31", features = ["bundled"] }
uuid = { version = "1", features = ["v4", "serde"] }

