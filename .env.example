# No environment variables are required for Phase 1 (core terminal-in-app).
# This file exists as a placeholder for future config (e.g. Phase 3 usage-adapter
# paths or API keys). Copy to .env and fill in values; .env is gitignored.
